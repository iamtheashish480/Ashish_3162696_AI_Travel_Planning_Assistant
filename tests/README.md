# Tests

The tests verify two important structural requirements:

- the MCP server exposes both required tools through the MCP protocol;
- the three knowledge-base source files and their metadata are present.

Run them with:

```bash
pytest -q
```
